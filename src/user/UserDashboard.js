import React from "react";
import Base from "../core/Base";

const UserDashboard = () => {
  return (
    <Base
      title="User Dashboard"
      description="Welcome to users dashboard"
    ></Base>
  );
};

export default UserDashboard;
