import React from "react";
import Base from "../core/Base";

const AdminDashboard = () => {
  return (
    <Base
      title="Admin Dashboard"
      description="Welcome to admins dashboard"
    ></Base>
  );
};

export default AdminDashboard;
